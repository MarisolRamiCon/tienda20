package com.inndata20.tienda.entity;

import jakarta.persistence.*;

@Entity
@Table

public class DetalleDePedidoEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id")
    private int id;
    @Column(name = )
}
